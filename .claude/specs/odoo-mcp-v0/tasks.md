# Tasks: Odoo 19 MCP Server (v0)

> Generated by spec-engine v2.3.0

<!-- Task lifecycle: pending -> in_progress -> completed
     Wired lifecycle: pending -> yes | n/a (for infra tasks with nothing to wire)
     Each task tracks: Status, Wave, Wired, Dependencies, Files
     Wave assignments are pre-computed by topological sort of dependency DAG.
     Tasks in the same wave have no mutual dependencies and can be batched.
     Files field lists which files this task will create or modify (enables parallel execution).
     Tasks in the same wave with non-overlapping Files can run in parallel.
     A task is only truly done when Status: completed AND Wired: yes (or n/a).
     Every implementation task MUST include at least one error-path acceptance criterion.
     IMPORTANT: Do NOT put wave numbers in section headers. The Wave: field is the sole source of truth. -->

Greenfield pnpm workspace. Two packages: `@netlinks/odoo-client` and `@netlinks/odoo-mcp`. All 24 tasks target `[NEW]` files. Wave 0 establishes shared type contracts and workspace scaffolding (4 tasks, fully parallel). Wave 1 builds JSON-RPC transport and PII sanitizer (2 tasks, parallel). Wave 2 builds auth abstraction (1 task). Wave 3 builds OdooClient and all odoo-mcp infrastructure (6 tasks, parallel across non-overlapping files). Wave 4 builds all 5 tool handlers and capability probe (6 tasks, parallel). Wave 5 wires tools and publishes resources (2 tasks, parallel). Wave 6 assembles the server factory and CLI (1 task). Wave 7 delivers smoke test and docs (2 tasks, parallel).

---

## Phase 1: Setup

### T-1: Define shared types and error classes

- **Status**: pending
- **Wave**: 0
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: none
- **Covers**: US-2 AC-3/AC-4, US-4 AC-7/AC-8, US-5 AC-5, US-7 AC-7/AC-8, US-8 AC-1/AC-2, US-10 AC-5
- **Wire into**: n/a
- **Files**:
  - `packages/odoo-client/src/types.ts` [NEW]
  - `packages/odoo-client/src/errors.ts` [NEW]
  - `packages/odoo-client/tests/errors.test.ts` [NEW]
- **Description**: Create the two foundational shared-contract files. `types.ts` exports verbatim: `DomainOperator`, `DomainLeaf`, `DomainConnector`, `Domain`, `Context`, `OdooRecord`, `OdooConfig`, `OdooSession`, `CompanyContext`, `ProbeResult` (10 exports). `errors.ts` exports the full error class hierarchy: `OdooError` (base with `errorType: string`, `model?: string`, `method?: string`, `traceback?: string`), `OdooAuthError`, `OdooUserError`, `OdooValidationError`, `OdooAccessError`, `OdooMissingError`, `OdooConnectionError` — each with constructor signatures exactly as designed. `OdooConnectionError` constructor accepts only `message: string` (no model/method/traceback). Both files must compile under `strict: true`, `module: Node16`, `target: ES2022`. No external imports.
- **Acceptance Criteria**:
  1. `tsc --noEmit` on both files succeeds with strict TypeScript ESM settings.
  2. `OdooConnectionError` constructor signature is `constructor(message: string)` — calling it with extra args causes a TypeScript error.
  3. `ProbeResult` fields `modules`, `reports`, `serverActions`, `companies`, `currencies` are typed as `Array<...> | { error: string }` — the union is not nullable and not `undefined`.
  4. Test file at `packages/odoo-client/tests/errors.test.ts` instantiates each of the 7 error classes (`OdooError`, `OdooAuthError`, `OdooUserError`, `OdooValidationError`, `OdooAccessError`, `OdooMissingError`, `OdooConnectionError`) and verifies that `errorType`, `message`, `model`, `method`, and `traceback` constructor params propagate correctly to instance properties; also verifies `OdooConnectionError` constructor rejects extra arguments at the TypeScript level.

---

### T-2: Workspace root scaffolding

- **Status**: pending
- **Wave**: 0
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: none
- **Covers**: US-10 AC-6
- **Wire into**: n/a
- **Files**:
  - `pnpm-workspace.yaml` [NEW]
  - `package.json` [NEW]
  - `tsconfig.base.json` [NEW]
  - `biome.json` [NEW]
  - `LICENSE` [NEW]
  - `.gitignore` [NEW]
- **Description**: Bootstrap the pnpm workspace root. `pnpm-workspace.yaml` declares `packages: ['packages/*']`. Root `package.json` has `"private": true`, scripts `build`, `test`, `lint` that delegate via `pnpm -r`, and ZERO `preinstall`/`install`/`postinstall` lifecycle scripts (US-10 AC-6). `tsconfig.base.json` has exactly these 13 compilerOptions fields: `strict: true`, `target: "ES2022"`, `module: "Node16"`, `moduleResolution: "Node16"`, `declaration: true`, `declarationMap: true`, `sourceMap: true`, `outDir: "dist"`, `rootDir: "src"`, `esModuleInterop: true`, `skipLibCheck: true`, `forceConsistentCasingInFileNames: true`, `resolveJsonModule: true`, `isolatedModules: true`. `biome.json` enables recommended lint + format rules. `LICENSE` is MIT with current year and NETLINKS copyright. `.gitignore` covers `node_modules/`, `dist/`, `*.js.map`, `.env`, `pnpm-lock.yaml` NOT listed (lockfile must be committed per US-10 AC-6).
- **Acceptance Criteria**:
  1. `pnpm install` runs without executing any lifecycle scripts.
  2. `grep -E '"(pre|post)?install"' package.json` returns zero matches.
  3. `pnpm-lock.yaml` is present after `pnpm install` and `.gitignore` does not exclude it.

---

### T-3: `@netlinks/odoo-client` package scaffolding

- **Status**: pending
- **Wave**: 0
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: none
- **Covers**: US-10 AC-1, US-10 AC-5, US-10 AC-6
- **Wire into**: n/a
- **Files**:
  - `packages/odoo-client/package.json` [NEW]
  - `packages/odoo-client/tsconfig.json` [NEW]
  - `packages/odoo-client/src/index.ts` [NEW]
- **Description**: Create `packages/odoo-client/package.json`: `name: "@netlinks/odoo-client"`, `version: "0.1.0"`, `type: "module"`, exports `"." -> { "import": "./dist/index.js", "types": "./dist/index.d.ts" }`, `files: ["dist"]`, `license: "MIT"`, `engines: { "node": ">=22" }`, zero runtime `dependencies`, `devDependencies: { "typescript": "^5.7", "vitest": "^3" }`, NO lifecycle scripts. `tsconfig.json` extends `"../../tsconfig.base.json"` with no additional overrides. `src/index.ts` barrel re-exports all named exports from `./types.js`, `./errors.js`, `./client.js`, `./auth.js`, `./rpc.js`, `./sanitize.js` (6 module re-exports).
- **Acceptance Criteria**:
  1. `packages/odoo-client/package.json` has zero entries in `dependencies` and zero lifecycle scripts.
  2. `packages/odoo-client/tsconfig.json` contains `"extends": "../../tsconfig.base.json"`.
  3. `grep -E '"(pre|post)?install"' packages/odoo-client/package.json` returns zero matches.

---

### T-4: `@netlinks/odoo-mcp` package scaffolding

- **Status**: pending
- **Wave**: 0
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: none
- **Covers**: US-10 AC-2, US-10 AC-4, US-10 AC-6
- **Wire into**: n/a
- **Files**:
  - `packages/odoo-mcp/package.json` [NEW]
  - `packages/odoo-mcp/tsconfig.json` [NEW]
  - `packages/odoo-mcp/src/index.ts` [NEW]
- **Description**: Create `packages/odoo-mcp/package.json`: `name: "@netlinks/odoo-mcp"`, `version: "0.1.0"`, `type: "module"`, `bin: { "odoo-mcp": "./dist/bin.js" }`, exports `"." -> { "import": "./dist/index.js", "types": "./dist/index.d.ts" }`, `files: ["dist"]`, `license: "MIT"`, `engines: { "node": ">=22" }`, `dependencies: { "@netlinks/odoo-client": "workspace:*", "@modelcontextprotocol/sdk": "^1", "zod": "^3" }`, `devDependencies: { "typescript": "^5.7", "vitest": "^3" }`, NO lifecycle scripts. `tsconfig.json` extends `"../../tsconfig.base.json"`. `src/index.ts` exports `createOdooMcpServer` from `"./server.js"` — this is the programmatic API entry point (US-10 AC-4).
- **Acceptance Criteria**:
  1. `packages/odoo-mcp/package.json` has `bin.odoo-mcp: "./dist/bin.js"` and zero lifecycle scripts.
  2. `packages/odoo-mcp/src/index.ts` re-exports `createOdooMcpServer` so `import { createOdooMcpServer } from '@netlinks/odoo-mcp'` resolves.
  3. `grep -E '"(pre|post)?install"' packages/odoo-mcp/package.json` returns zero matches.

---

## Phase 2: Core Implementation

### T-5: JSON-RPC transport

- **Status**: pending
- **Wave**: 1
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: T-1
- **Covers**: US-2 AC-1, US-4 AC-1-6, US-5 AC-1-3, US-6 AC-1, US-8 AC-4, US-8 AC-7
- **Wire into**: n/a
- **Files**:
  - `packages/odoo-client/src/rpc.ts` [NEW]
  - `packages/odoo-client/tests/rpc.test.ts` [NEW]
- **Description**: Implement `JsonRpcRequest`, `JsonRpcResponse` interfaces and `jsonRpc(url: string, endpoint: string, params: Record<string, unknown>, headers?: Record<string, string>): Promise<unknown>` using Node.js 22 native `fetch`. Build request body as `{ jsonrpc: '2.0', method: 'call', id: <monotonic-counter>, params }`. POST to `${url}${endpoint}` with `Content-Type: application/json` plus any provided headers. Threat-model (US-8 AC-7): apply a 30-second `AbortController` timeout to every fetch; on abort throw `new OdooConnectionError('Request timeout after 30s')`. Parse response JSON as `JsonRpcResponse`. If `response.error` is present, map `response.error.data.name` to the appropriate subclass: `odoo.exceptions.UserError` -> `OdooUserError`, `odoo.exceptions.ValidationError` -> `OdooValidationError`, `odoo.exceptions.AccessError` -> `OdooAccessError`, `odoo.exceptions.MissingError` -> `OdooMissingError`, `odoo.exceptions.AccessDenied` -> `OdooAuthError`, any other value -> `OdooError` with `errorType: 'ServerError'`. Pass `response.error.data.message` as the error message and `response.error.data.debug` as `traceback`. Catch fetch network errors and rethrow as `new OdooConnectionError(originalError.message)`. Tests use `vi.stubGlobal('fetch', mockFn)`.
- **Acceptance Criteria**:
  1. A mocked successful response returns `response.result` unwrapped.
  2. A mocked `odoo.exceptions.AccessError` fault throws an instance of `OdooAccessError` with the Odoo message verbatim.
  3. A mocked fetch that never resolves triggers the 30-second timeout and throws `OdooConnectionError` with message containing "timeout".
  4. A fetch that rejects (DNS failure) throws `OdooConnectionError` wrapping the original error message.
  5. An unrecognized error name (e.g., `odoo.custom.Error`) throws `OdooError` with `errorType: 'ServerError'`.
  6. Test file at `packages/odoo-client/tests/rpc.test.ts` with at least 6 test cases.

---

### T-6: Auth strategy abstraction

- **Status**: pending
- **Wave**: 2
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: T-1, T-5
- **Covers**: US-2 AC-1, US-2 AC-2, US-2 AC-3, US-2 AC-5
- **Wire into**: n/a
- **Files**:
  - `packages/odoo-client/src/auth.ts` [NEW]
  - `packages/odoo-client/tests/auth.test.ts` [NEW]
- **Description**: Implement `AuthStrategy` interface with `authenticate(config: OdooConfig): Promise<OdooSession>` and `applyAuth(request: JsonRpcRequest, session: OdooSession): JsonRpcRequest`. Implement `ApiKeyAuthStrategy`: `authenticate` calls `jsonRpc(config.url, '/web/session/authenticate', { db: config.db, login: config.username, password: config.apiKey })`, extracts `result.uid`, `result.company_id` -> `companyId`, `result.allowed_company_ids` -> `allowedCompanyIds`, `result.user_context` -> `userContext`, returns `OdooSession`. `applyAuth` attaches `session_id` to request headers if present. Implement `SessionCookieAuthStrategy` as a fallback (similar authenticate flow, stores cookie). Implement `createAuthStrategy(config: OdooConfig): Promise<AuthStrategy>`: tries `ApiKeyAuthStrategy` first; if it throws `OdooAuthError`, retries with `SessionCookieAuthStrategy`; if both fail, rethrows the last error. Threat-model (US-2 AC-5): at the top of `createAuthStrategy`, if `config.url.startsWith('http://')`, write a structured warning JSON line to `process.stderr` before any RPC call — `{ event: "warning", message: "ODOO_URL uses http:// — credentials transmitted in plaintext" }`.
- **Acceptance Criteria**:
  1. A mocked successful authenticate response returns `OdooSession` with `uid`, `companyId`, `allowedCompanyIds`, `userContext` all populated.
  2. A mocked `AccessDenied` fault from Odoo causes `authenticate` to throw `OdooAuthError`.
  3. When `config.url` starts with `http://`, a JSON warning line is written to stderr before any RPC call.
  4. `createAuthStrategy` falls back to `SessionCookieAuthStrategy` when `ApiKeyAuthStrategy` throws `OdooAuthError`.
  5. Test file at `packages/odoo-client/tests/auth.test.ts` with at least 5 test cases.

---

### T-7: `OdooClient` class

- **Status**: pending
- **Wave**: 3
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: T-1, T-5, T-6
- **Covers**: US-2, US-3, US-4, US-5, US-6, US-8
- **Wire into**: n/a
- **Files**:
  - `packages/odoo-client/src/client.ts` [NEW]
  - `packages/odoo-client/tests/client.test.ts` [NEW]
- **Description**: Implement `OdooClient` class matching the verified interface exactly. Constructor stores `config: OdooConfig`. `authenticate()` calls `createAuthStrategy(config)` then `strategy.authenticate(config)`, stores `this.session` and `this.strategy` internally, returns `OdooSession`. ORM methods call `jsonRpc` at `/web/dataset/call_kw` with the standard call_kw body shape `{ model, method, args, kwargs: { ...kwargs, context } }`. `searchRead` default `limit: 80` when not provided (US-4 AC-9). `read` -> `read`. `create` -> `create`. `write` -> `write`. `unlink` -> `unlink`. `searchCount` -> `search_count`. `execute(model, method, args, kwargs?, context?)` -> `execute_kw`. `runReport(reportId, docIds, context?)` calls the Odoo 19 report render endpoint `render_qweb_pdf` (or `_render_qweb_pdf` if 19.x renames it); returns `{ content: base64String, contentType: 'application/pdf' }`. `callAction(model, ids, actionName, context?)` calls the named method on the model with the ids as positional args. `fieldsGet(model, attributes?, context?)` calls `fields_get` with `{ allfields: true }` or attributes as kwargs. `probe()` runs 8 sub-queries via `Promise.allSettled` — never throws for individual failures, populates failed fields with `{ error: message }` (US-3 AC-3/AC-4/AC-6). Network errors from `jsonRpc` (which are already `OdooError` subclasses) propagate up unchanged. Tests mock `jsonRpc` at module level using `vi.mock`.
- **Acceptance Criteria**:
  1. `searchRead('res.partner', [], ['name'])` with no options sends a JSON-RPC request with `method: 'search_read'` and `kwargs.limit === 80`.
  2. `probe()` with all 8 sub-queries mocked to throw returns a `ProbeResult` where all 8 fields are `{ error: string }` objects — no thrown exception, no empty arrays.
  3. `probe()` with 3 sub-queries failing populates the 3 failed fields with `{ error: string }` and the 5 successful fields normally.
  4. `authenticate()` throws `OdooAuthError` when the auth strategy throws.
  5. Test file at `packages/odoo-client/tests/client.test.ts` with at least 10 test cases covering all public methods.

---

### T-8: PII sanitizer

- **Status**: pending
- **Wave**: 1
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: T-1
- **Covers**: US-9 AC-2, US-9 AC-3
- **Wire into**: n/a
- **Files**:
  - `packages/odoo-client/src/sanitize.ts` [NEW]
- **Description**: Export `sanitizeArgs(toolName: string, args: Record<string, unknown>): Record<string, unknown>`. For `create` and `write` tool calls: deep-clone `args.values` (using `structuredClone`), replace any key matching `/password|credit_card|token|secret|api_key/i` with `"[REDACTED]"` recursively. Return the sanitized copy with the rest of `args` unchanged; never mutate the original args object. For all other tool names, return `args` as-is. No external imports — pure built-in TypeScript.
- **Acceptance Criteria**:
  1. `sanitizeArgs('odoo_create', { model: 'res.partner', values: { name: 'Test', password: 'secret123' } })` returns `values.password === '[REDACTED]'` and `values.name === 'Test'`.
  2. `sanitizeArgs('odoo_search_read', { model: 'res.partner', domain: [] })` returns the args object unchanged.
  3. `sanitizeArgs('odoo_write', { model: 'x', ids: [1], values: { nested: { api_key: 'abc' } } })` returns `values.nested.api_key === '[REDACTED]'` (deep traversal).

---

### T-9: Config loader

- **Status**: pending
- **Wave**: 3
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: T-1, T-4
- **Covers**: US-1 AC-1, US-1 AC-2, US-1 AC-3, US-1 AC-4, US-1 AC-5, US-1 AC-6
- **Wire into**: n/a
- **Files**:
  - `packages/odoo-mcp/src/config.ts` [NEW]
  - `packages/odoo-mcp/tests/config.test.ts` [NEW]
- **Description**: Implement `loadConfig(): AppConfig` using the Zod `configSchema`: `ODOO_URL` (string URL, trailing slash stripped via `.transform(u => u.replace(/\/$/, ''))`), `ODOO_DB` (non-empty string), `ODOO_USERNAME` (non-empty string), `ODOO_API_KEY` (non-empty string), `ODOO_MCP_LOG_FILE` (optional string). On Zod parse failure: collect all missing/invalid variable names, write `JSON.stringify({ event: 'config_error', missing: string[], invalid: string[] })` to `process.stderr`, then call `process.exit(1)`. Threat-model (US-1 AC-6): the structured error payload MUST NOT contain the value of `ODOO_API_KEY` — only the key name string `"ODOO_API_KEY"` if it is missing or invalid. When `ODOO_MCP_LOG_FILE` is set, verify the directory is writable by attempting `fs.openSync(logFile, 'a')` and immediately closing the fd; on any error write structured error to stderr and `process.exit(1)` (US-1 AC-5). `loadConfig` returns `AppConfig: { odoo: OdooConfig, logFile?: string }`. The `http://` URL check is NOT done here — only `https?://.+` pattern validation via Zod's `.url()` (US-1 AC-3).
- **Acceptance Criteria**:
  1. All 4 required env vars present: `loadConfig()` returns `AppConfig` with `odoo.url` having no trailing slash.
  2. `ODOO_URL` missing: `process.exit(1)` is called and the stderr JSON contains `"ODOO_URL"` in the missing list but does NOT contain the value of `ODOO_API_KEY` as a substring anywhere in the output.
  3. `ODOO_URL: "not-a-url"`: `process.exit(1)` called, stderr JSON identifies `ODOO_URL` as invalid.
  4. Unwritable `ODOO_MCP_LOG_FILE` path: `process.exit(1)` called with structured error.
  5. Test file at `packages/odoo-mcp/tests/config.test.ts` with at least 7 test cases; one test asserts the error string does not contain a sentinel API key value such as `"test-api-key-value"`.

---

### T-10: Structured logger

- **Status**: pending
- **Wave**: 3
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: T-4
- **Covers**: US-9 AC-1, US-9 AC-2, US-9 AC-4, US-9 AC-5, US-9 AC-6, US-9 AC-7
- **Wire into**: n/a
- **Files**:
  - `packages/odoo-mcp/src/logger.ts` [NEW]
- **Description**: Implement `createLogger(logFile?: string): Logger`. `Logger` interface has 3 methods: `toolCall(entry: { tool: string; args_sanitized: Record<string, unknown>; latency_ms: number; status: 'ok' | 'error'; error?: string }): void`, `startup(info: { odoo_url: string; odoo_db: string; odoo_username: string }): void`, `shutdown(): void`. Each method serializes a JSON object with `ts: new Date().toISOString()` prepended and writes the line to `process.stderr` via `process.stderr.write`. When `logFile` is set, ALSO write the same line to the file. Threat-model (US-9 AC-7): create the log file with permissions `0o600` using `fs.openSync(logFile, 'a', 0o600)` — this sets `0o600` only on file creation; subsequent appends use the returned fd. `startup` emits `{ ts, event: 'startup', odoo_url, odoo_db, odoo_username }` — the three odoo_* fields do NOT include `odoo_api_key` (US-9 AC-2). `toolCall` emits `{ ts, event: 'tool_call', tool, args_sanitized, latency_ms, status, error? }`. `shutdown` emits `{ ts, event: 'shutdown' }`. All log goes to stderr only (stdout is reserved for MCP stdio transport). No external logging library.
- **Acceptance Criteria**:
  1. `createLogger()` (no file) writes each log entry to stderr only.
  2. `createLogger('/tmp/odoo-test.log')` creates the file with `0o600` permissions and writes each log entry to both stderr and the file.
  3. `startup({ odoo_url: 'https://x', odoo_db: 'db', odoo_username: 'admin' })` emits a line with `event: 'startup'` that does not contain the string `"api_key"` anywhere.
  4. `shutdown()` emits a line containing `event: 'shutdown'`.

---

### T-11: MCP error formatter

- **Status**: pending
- **Wave**: 3
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: T-1, T-4
- **Covers**: US-8 AC-1, US-8 AC-2, US-8 AC-3, US-8 AC-5, US-8 AC-6
- **Wire into**: n/a
- **Files**:
  - `packages/odoo-mcp/src/errors.ts` [NEW]
  - `packages/odoo-mcp/tests/errors.test.ts` [NEW]
- **Description**: Export `McpToolError` interface (`{ error_type: string; message: string; model?: string; method?: string; traceback?: string }`) and `formatMcpError(error: OdooError): McpToolError`. Map `error.errorType` -> `error_type`, `error.message` verbatim (US-8 AC-3), `error.model` -> `model?`, `error.method` -> `method?`. Threat-model (US-8 AC-6): include `traceback` field ONLY when `process.env.ODOO_MCP_DEBUG === '1'`; when debug is off, omit the field entirely from the returned object (do not set it to `undefined` — delete it or never set it so JSON serialization excludes it). Errors from Odoo are never rewritten or interpreted — pass through verbatim. Tests cover debug mode on/off for all 6 error subclasses.
- **Acceptance Criteria**:
  1. `formatMcpError(new OdooUserError('bad input', 'res.partner', 'write', 'traceback-text'))` with `ODOO_MCP_DEBUG` unset returns an object with no `traceback` key.
  2. Same call with `ODOO_MCP_DEBUG=1` returns `{ ..., traceback: 'traceback-text' }`.
  3. `result.message === error.message` — message is never rewritten.
  4. Test file at `packages/odoo-mcp/tests/errors.test.ts` covers all 6 error subclass types (`OdooAuthError`, `OdooUserError`, `OdooValidationError`, `OdooAccessError`, `OdooMissingError`, `OdooConnectionError`).

---

### T-12: Multi-company context builder

- **Status**: pending
- **Wave**: 3
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: T-1, T-4
- **Covers**: US-7 AC-1, US-7 AC-2, US-7 AC-3, US-7 AC-4, US-7 AC-5, US-7 AC-7, US-7 AC-8
- **Wire into**: n/a
- **Files**:
  - `packages/odoo-mcp/src/context.ts` [NEW]
  - `packages/odoo-mcp/tests/context.test.ts` [NEW]
- **Description**: Export `buildContext(session: OdooSession, companyArgs: CompanyContext, extraContext?: Context): Context` and `validateCompanySubset(callerIds: number[], sessionIds: number[]): void`. `buildContext` logic: (1) start with `{ ...session.userContext }`; (2) set `allowed_company_ids = companyArgs.allowed_company_ids ?? session.allowedCompanyIds`; (3) set `company_id = companyArgs.active_company_id ?? session.companyId`; (4) set `uid = session.uid`; (5) merge `extraContext` if provided: `{ ...accumulated, ...extraContext }`; (6) re-apply `uid`, `allowed_company_ids`, `company_id` from step 2-4 to override any extraContext values — this is the threat-model enforcement (US-7 AC-7). `validateCompanySubset(callerIds, sessionIds)`: if any id in `callerIds` is not present in `sessionIds`, throw `new OdooAccessError('InputValidationError: company ID not in session allowedCompanyIds')` with the message identifying the offending IDs (US-7 AC-8). Note: the error_type used by `formatMcpError` will read `error.errorType` which for `OdooAccessError` is `'AccessError'`; to surface `'InputValidationError'` to Claude, throw `new OdooError('InputValidationError', message)` instead — use `OdooError` base directly with custom `errorType`.
- **Acceptance Criteria**:
  1. `buildContext(session, {}, { uid: 999 })` returns context with `uid === session.uid` (not 999).
  2. `buildContext(session, {}, { allowed_company_ids: [999] })` returns context with `allowed_company_ids === session.allowedCompanyIds`.
  3. `validateCompanySubset([1, 2], [1, 2, 3])` does not throw.
  4. `validateCompanySubset([1, 99], [1, 2, 3])` throws an `OdooError` with `errorType === 'InputValidationError'`.
  5. Test file at `packages/odoo-mcp/tests/context.test.ts` with at least 6 test cases.

---

### T-13: Zod tool input schemas

- **Status**: pending
- **Wave**: 3
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: T-4
- **Covers**: US-4 AC-9, US-4 AC-10, US-7 AC-1
- **Wire into**: n/a
- **Files**:
  - `packages/odoo-mcp/src/tools/schemas.ts` [NEW]
- **Description**: Implement all 10 Zod schemas exactly as designed, plus inferred TypeScript types via `z.infer<>`. Shared `companyContext` object: `{ allowed_company_ids: z.array(z.number().int().positive()).optional(), active_company_id: z.number().int().positive().optional() }`. Export `searchReadSchema` (model min 1, domain default `[]`, fields default `[]`, limit int positive default 80, offset int nonneg default 0, order optional string, spread companyContext), `readSchema` (model, ids array min 1, fields default `[]`, companyContext), `createSchema` (model, values union of record or array of records, companyContext), `writeSchema` (model, ids array min 1, values record, companyContext), `unlinkSchema` (model, ids array min 1, companyContext), `searchCountSchema` (model, domain default `[]`, companyContext), `executeSchema` (model, method, args default `[]`, kwargs record default `{}`, companyContext), `runReportSchema` (report_id union of int or string, doc_ids array min 1, companyContext), `callActionSchema` (model, ids array min 1, action_name, context optional record, companyContext), `fieldsGetSchema` (model, attributes optional string array, companyContext). Export inferred types: `SearchReadInput`, `ReadInput`, `CreateInput`, `WriteInput`, `UnlinkInput`, `SearchCountInput`, `ExecuteInput`, `RunReportInput`, `CallActionInput`, `FieldsGetInput`.
- **Acceptance Criteria**:
  1. `searchReadSchema.parse({ model: 'res.partner' })` returns `{ model: 'res.partner', domain: [], fields: [], limit: 80, offset: 0 }` with `allowed_company_ids` and `active_company_id` absent (undefined).
  2. `searchReadSchema.parse({ model: '' })` throws a ZodError.
  3. `executeSchema.parse({ model: 'res.partner', method: 'do_something' })` returns `args: []` and `kwargs: {}`.
  4. All 10 schema exports and all 10 inferred type exports are present in the module.

---

### T-14: ORM tool handlers

- **Status**: pending
- **Wave**: 4
- **Wired**: pending
- **Deprecates**: none
- **Dependencies**: T-1, T-4, T-7, T-8, T-11, T-12, T-13
- **Covers**: US-4 AC-1-10, US-7 AC-1-8
- **Wire into**: n/a
- **Files**:
  - `packages/odoo-mcp/src/tools/orm.ts` [NEW]
  - `packages/odoo-mcp/tests/tools/orm.test.ts` [NEW]
- **Description**: Export `registerOrmTools(server: McpServer, client: OdooClient, session: OdooSession, logger: Logger): void` registering 6 MCP tools: `odoo_search_read`, `odoo_read`, `odoo_create`, `odoo_write`, `odoo_unlink`, `odoo_search_count`. Each handler follows this sequence: (1) parse args with the corresponding schema from `schemas.ts`; (2) if `allowed_company_ids` provided, call `validateCompanySubset(args.allowed_company_ids, session.allowedCompanyIds)` — on error return `{ content: [{ type: 'text', text: JSON.stringify(formatMcpError(e)) }], isError: true }`; (3) call `buildContext(session, { allowed_company_ids: args.allowed_company_ids, active_company_id: args.active_company_id })`; (4) call the corresponding `OdooClient` method; (5) on success return `{ content: [{ type: 'text', text: JSON.stringify(result) }], isError: false }` and call `logger.toolCall({ tool, args_sanitized: sanitizeArgs(tool, args), latency_ms, status: 'ok' })`; (6) on `OdooError` return `{ content: [{ type: 'text', text: JSON.stringify(formatMcpError(e)) }], isError: true }` and log `status: 'error'`. Zod validation failures are returned as `isError: true` with `error_type: 'InputValidationError'` without calling the client. Tests mock `OdooClient`, `buildContext`, and `Logger`.
- **Acceptance Criteria**:
  1. `odoo_search_read` with `{ model: 'res.partner', fields: ['name'], limit: 10 }` calls `client.searchRead('res.partner', [], ['name'], { limit: 10, ... })`.
  2. `odoo_search_read` with missing `model` returns `isError: true` with `error_type: 'InputValidationError'` without calling `client.searchRead`.
  3. `odoo_create` with `values: { password: 'x' }` logs `args_sanitized` with `values.password: '[REDACTED]'`.
  4. `odoo_search_read` with `allowed_company_ids: [999]` when `session.allowedCompanyIds: [1]` returns `isError: true` with `error_type: 'InputValidationError'` and does not call the client.
  5. `client.searchRead` throwing `OdooAccessError` returns `isError: true` with `error_type: 'AccessError'`.
  6. Test file at `packages/odoo-mcp/tests/tools/orm.test.ts` with at least 10 test cases.

---

### T-15: Execute tool handler

- **Status**: pending
- **Wave**: 4
- **Wired**: pending
- **Deprecates**: none
- **Dependencies**: T-1, T-4, T-11, T-12, T-13
- **Covers**: US-5 AC-1, US-5 AC-5, US-5 AC-9
- **Wire into**: n/a
- **Files**:
  - `packages/odoo-mcp/src/tools/execute.ts` [NEW]
  - `packages/odoo-mcp/tests/tools/execute.test.ts` [NEW]
- **Description**: Export `registerExecuteTool(server: McpServer, client: OdooClient, session: OdooSession, logger: Logger): void` registering `odoo_execute`. Handler: (1) parse with `executeSchema`; (2) threat-model (US-5 AC-9) — validate `args.model` against `/^[a-z][a-z0-9_.]*$/` and `args.method` against `/^[a-z_][a-z0-9_]*$/`; if either fails return `{ content: [{ type: 'text', text: JSON.stringify({ error_type: 'InputValidationError', message: 'model or method contains invalid characters' }) }], isError: true }` immediately without calling the client; (3) if `allowed_company_ids` provided, call `validateCompanySubset`; (4) `buildContext`; (5) call `client.execute(model, method, args, kwargs, context)`; (6) return result as JSON text; (7) `logger.toolCall`; (8) on `OdooError`, `formatMcpError` + `isError: true`.
- **Acceptance Criteria**:
  1. `odoo_execute` with `{ model: 'res.partner', method: 'write', args: [], kwargs: {} }` calls `client.execute` and returns result.
  2. `odoo_execute` with `model: 'Res.Partner'` (uppercase R) returns `isError: true` with `error_type: 'InputValidationError'` without calling `client.execute`.
  3. `odoo_execute` with `method: 'My-Method'` returns `isError: true` with `error_type: 'InputValidationError'`.
  4. `client.execute` throwing `OdooUserError` returns `isError: true` with `error_type: 'UserError'`.
  5. Test file at `packages/odoo-mcp/tests/tools/execute.test.ts` with at least 6 test cases.

---

### T-16: Report tool handler

- **Status**: pending
- **Wave**: 4
- **Wired**: pending
- **Deprecates**: none
- **Dependencies**: T-1, T-4, T-11, T-12, T-13
- **Covers**: US-5 AC-2, US-5 AC-6, US-5 AC-8
- **Wire into**: n/a
- **Files**:
  - `packages/odoo-mcp/src/tools/report.ts` [NEW]
  - `packages/odoo-mcp/tests/tools/report.test.ts` [NEW]
- **Description**: Export `registerReportTool(server: McpServer, client: OdooClient, session: OdooSession, logger: Logger): void` registering `odoo_run_report`. Handler: (1) parse with `runReportSchema`; (2) if `allowed_company_ids` provided, `validateCompanySubset`; (3) `buildContext`; (4) call `client.runReport(report_id, doc_ids, context)` which returns `{ content: string, contentType: string }`; (5) return `JSON.stringify({ content, contentType })` as text; (6) `logger.toolCall`. The handler does not know Odoo's internal report protocol — that is abstracted in `client.runReport` (US-5 AC-8). On `OdooError`, `formatMcpError` + `isError: true`.
- **Acceptance Criteria**:
  1. `odoo_run_report` with valid `report_id: 1` and `doc_ids: [42]` calls `client.runReport(1, [42], context)` and returns `{ content, contentType }` in the response.
  2. `client.runReport` throwing `OdooUserError` (non-existent report) returns `isError: true` with the Odoo message verbatim.
  3. Test file at `packages/odoo-mcp/tests/tools/report.test.ts` with at least 4 test cases.

---

### T-17: Action tool handler

- **Status**: pending
- **Wave**: 4
- **Wired**: pending
- **Deprecates**: none
- **Dependencies**: T-1, T-4, T-11, T-12, T-13
- **Covers**: US-5 AC-3, US-5 AC-4, US-5 AC-7
- **Wire into**: n/a
- **Files**:
  - `packages/odoo-mcp/src/tools/action.ts` [NEW]
  - `packages/odoo-mcp/tests/tools/action.test.ts` [NEW]
- **Description**: Export `registerActionTool(server: McpServer, client: OdooClient, session: OdooSession, logger: Logger): void` registering `odoo_call_action`. Handler: (1) parse with `callActionSchema`; (2) if `allowed_company_ids` provided, `validateCompanySubset`; (3) merge caller `context` with company context via `buildContext(session, { allowed_company_ids: args.allowed_company_ids, active_company_id: args.active_company_id }, args.context)` — the `extraContext` parameter accepts caller-provided context but `buildContext` prevents override of uid/company fields (US-5 AC-4 + US-7 AC-7); (4) call `client.callAction(model, ids, action_name, context)`; (5) return result as JSON; (6) `logger.toolCall`. On `OdooError`, `formatMcpError` + `isError: true`.
- **Acceptance Criteria**:
  1. `odoo_call_action` with `context: { active_test: false }` passes that key through in the merged context while preserving `uid` and `company_id` from session.
  2. `client.callAction` throwing (action not found on model) returns `isError: true` with the Odoo fault message.
  3. Test file at `packages/odoo-mcp/tests/tools/action.test.ts` with at least 4 test cases.

---

### T-18: Fields-get tool handler

- **Status**: pending
- **Wave**: 4
- **Wired**: pending
- **Deprecates**: none
- **Dependencies**: T-1, T-4, T-11, T-12, T-13
- **Covers**: US-6 AC-1, US-6 AC-2, US-6 AC-3
- **Wire into**: n/a
- **Files**:
  - `packages/odoo-mcp/src/tools/introspect.ts` [NEW]
  - `packages/odoo-mcp/tests/tools/introspect.test.ts` [NEW]
- **Description**: Export `registerIntrospectTool(server: McpServer, client: OdooClient, session: OdooSession, logger: Logger): void` registering `odoo_fields_get`. Handler: (1) parse with `fieldsGetSchema`; (2) `buildContext`; (3) call `client.fieldsGet(model, args.attributes, context)` — passes `attributes` as-is (undefined if not provided, which causes `client.fieldsGet` to omit the attribute filter per US-6 AC-2); (4) return result as `JSON.stringify(result)` text; (5) `logger.toolCall`. On `OdooError` (non-existent model returns `MissingError` or `AccessError`), `formatMcpError` + `isError: true`.
- **Acceptance Criteria**:
  1. `odoo_fields_get` with `{ model: 'res.partner' }` (no attributes) calls `client.fieldsGet('res.partner', undefined, context)`.
  2. `odoo_fields_get` with `{ model: 'res.partner', attributes: ['string', 'type'] }` calls `client.fieldsGet('res.partner', ['string', 'type'], context)`.
  3. `client.fieldsGet` throwing (non-existent model) returns `isError: true` with the Odoo fault details.
  4. Test file at `packages/odoo-mcp/tests/tools/introspect.test.ts` with at least 4 test cases.

---

### T-19: Capability probe

- **Status**: pending
- **Wave**: 4
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: T-1, T-4, T-7
- **Covers**: US-3 AC-1, US-3 AC-3, US-3 AC-4, US-3 AC-6
- **Wire into**: n/a
- **Files**:
  - `packages/odoo-mcp/src/probe.ts` [NEW]
  - `packages/odoo-mcp/tests/probe.test.ts` [NEW]
- **Description**: Implement `runProbe(client: OdooClient): Promise<ProbeResult>`. Run all 8 sub-queries via `Promise.allSettled` in parallel: (1) `client.searchRead('ir.module.module', [['state','=','installed']], ['name','version'], { limit: 500 })` -> `modules`; (2) `client.searchRead('ir.actions.report', [], ['report_name','model','report_type'])` -> `reports`; (3) `client.searchRead('ir.actions.server', [], ['name','model_id','type'])` -> `serverActions` (map `model_id[1]` to `model` string); (4) `client.searchRead('res.company', [], ['id','name','currency_id'])` -> `companies`; (5) `client.searchRead('res.currency', [['active','=',true]], ['id','name','symbol'])` -> `currencies`; (6) read fiscal year from `account.fiscal.year` or fall back to `res.company` fields `date_from`/`date_to` -> `fiscalYear: { date_from, date_to }`; (7) extract language from `client` session's `userContext.lang` -> `language: string`; (8) extract locale from `userContext.lang` or `userContext.locale` -> `locale: string`. For any sub-query that rejects, set that `ProbeResult` field to `{ error: rejectedReason.message }`. Threat-model (US-3 AC-6): if ALL 8 sub-queries reject, write `JSON.stringify({ event: 'warning', message: 'All probe sub-queries failed' })` to `process.stderr` — still return the all-error `ProbeResult`, never throw, never return empty arrays.
- **Acceptance Criteria**:
  1. All 8 sub-queries succeed: `ProbeResult` has all 8 fields populated as arrays or string values (no `{ error }` field).
  2. `ir.actions.report` sub-query rejects: `result.reports === { error: '...' }`, other 7 fields unaffected.
  3. All 8 sub-queries reject: all 8 fields are `{ error: string }`, function resolves (not rejects), writes warning to stderr.
  4. Test file at `packages/odoo-mcp/tests/probe.test.ts` with at least 6 test cases.

---

### T-20: MCP resource publisher

- **Status**: pending
- **Wave**: 5
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: T-1, T-4, T-19
- **Covers**: US-3 AC-2, US-3 AC-5
- **Wire into**: n/a
- **Files**:
  - `packages/odoo-mcp/src/resources.ts` [NEW]
- **Description**: Implement `registerResources(server: McpServer, probe: ProbeResult): void`. Register 7 MCP resources using the MCP SDK `server.resource(uri, handler)` API with stable URIs: `odoo://modules`, `odoo://reports`, `odoo://server-actions`, `odoo://companies`, `odoo://currencies`, `odoo://fiscal-year`, `odoo://user-context`. Each resource handler is a closure over the `probe` argument that returns the corresponding field as a `text/plain` JSON string — no re-query to Odoo (US-3 AC-5). If a probe field is `{ error: string }`, the resource returns that JSON object as the content body (US-3 AC-3). Resource descriptions name what each contains. Map: `odoo://modules` -> `probe.modules`, `odoo://reports` -> `probe.reports`, `odoo://server-actions` -> `probe.serverActions`, `odoo://companies` -> `probe.companies`, `odoo://currencies` -> `probe.currencies`, `odoo://fiscal-year` -> `probe.fiscalYear`, `odoo://user-context` -> `{ language: probe.language, locale: probe.locale }`.
- **Acceptance Criteria**:
  1. After `registerResources(server, probe)`, the server has exactly 7 registered resources.
  2. Reading `odoo://modules` when probe succeeded returns the modules array as a JSON string.
  3. Reading `odoo://reports` when `probe.reports === { error: 'forbidden' }` returns `{ "error": "forbidden" }` as content — not an empty array, not an MCP error response.

---

## Phase 3: Integration

### T-21: Tool registration orchestrator

- **Status**: pending
- **Wave**: 5
- **Wired**: pending
- **Deprecates**: none
- **Dependencies**: T-14, T-15, T-16, T-17, T-18
- **Covers**: US-4, US-5, US-6, US-7
- **Wire into**: n/a
- **Files**:
  - `packages/odoo-mcp/src/tools/index.ts` [NEW]
- **Description**: Implement `registerAllTools(server: McpServer, client: OdooClient, session: OdooSession, logger: Logger): void`. This file is the single wiring point that aggregates all 5 tool-group register functions. Import `registerOrmTools` from `./orm.js`, `registerExecuteTool` from `./execute.js`, `registerReportTool` from `./report.js`, `registerActionTool` from `./action.js`, and `registerIntrospectTool` from `./introspect.js` — exactly these 5 imports, no others. Call each in sequence: `registerOrmTools(server, client, session, logger)`, `registerExecuteTool(server, client, session, logger)`, `registerReportTool(server, client, session, logger)`, `registerActionTool(server, client, session, logger)`, `registerIntrospectTool(server, client, session, logger)`. No other logic. The individual tool handlers (T-14..T-18) do NOT wire themselves — T-21 is the sole wiring point for `tools/index.ts`.
- **Acceptance Criteria**:
  1. After `registerAllTools`, the MCP server has exactly 10 registered tools.
  2. The file imports from `./orm.js`, `./execute.js`, `./report.js`, `./action.js`, `./introspect.js` (5 imports, no others).

---

### T-22: MCP server factory and CLI entry point

- **Status**: pending
- **Wave**: 6
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: T-9, T-10, T-19, T-20, T-21
- **Covers**: US-1 AC-1-5, US-2 AC-1-5, US-3 AC-1-6, US-9 AC-5, US-10 AC-2, US-10 AC-3, US-10 AC-4
- **Wire into**: n/a
- **Files**:
  - `packages/odoo-mcp/src/server.ts` [NEW]
  - `packages/odoo-mcp/src/bin.ts` [NEW]
  - `packages/odoo-mcp/tests/server.test.ts` [NEW]
- **Description**: Implement `createOdooMcpServer(config: McpServerConfig): Promise<McpServer>` in `server.ts`. This file is responsible for wiring together all subsystems including the tool registration orchestrator. Import `registerAllTools` from `./tools/index.js`. Sequence: (1) create `new OdooClient(config.odooConfig)`; (2) call `client.authenticate()` — throws `OdooAuthError` on failure, caller handles; (3) create `logger = createLogger(config.logFile)`; (4) call `runProbe(client)` to get `probe: ProbeResult`; (5) create `server = new McpServer({ name: 'odoo-mcp', version: '0.1.0' })` from `@modelcontextprotocol/sdk`; (6) call `registerResources(server, probe)`; (7) call `registerAllTools(server, client, session, logger)` — this is where `registerAllTools` from `./tools/index.js` is invoked during `createOdooMcpServer`; (8) return `server`. Implement `bin.ts` as the CLI entry point: shebang `#!/usr/bin/env node`, then: (1) `loadConfig()`; (2) `createOdooMcpServer(config)`; (3) `logger.startup({ odoo_url: config.odoo.url, odoo_db: config.odoo.db, odoo_username: config.odoo.username })`; (4) `await server.connect(new StdioServerTransport())`; (5) register `process.on('SIGTERM', ...)` and `process.on('SIGINT', ...)` handlers that call `logger.shutdown()` then `process.exit(0)`. Wrap the whole `bin.ts` body in `try/catch` — on any uncaught error during startup, `JSON.stringify` the error to stderr and `process.exit(1)`. Server tests mock `OdooClient`, `runProbe`, `registerResources`, and `registerAllTools`.
- **Acceptance Criteria**:
  1. `createOdooMcpServer` with a successful mock returns a `McpServer` instance.
  2. `createOdooMcpServer` where `client.authenticate()` throws `OdooAuthError` propagates the error (does not swallow it); `bin.ts` catches it, writes to stderr, exits 1.
  3. `bin.ts` calls `logger.startup` before `server.connect`.
  4. `SIGTERM` triggers `logger.shutdown()` before exit.
  5. Test file at `packages/odoo-mcp/tests/server.test.ts` with at least 5 test cases.

---

## Phase 4: End-to-End Testing

### T-23: Smoke test script

- **Status**: pending
- **Wave**: 7
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: T-22
- **Covers**: US-10 AC-3
- **Wire into**: n/a
- **Files**:
  - `scripts/smoke-test.mjs` [NEW]
- **Description**: Write a Node.js ESM script. Spawn `node packages/odoo-mcp/dist/bin.js` with `child_process.spawn`, forwarding `process.env`. Write MCP `initialize` message then `tools/list` request to stdin (MCP JSON-RPC protocol). Read stdout line-by-line; assert: (a) `initialize` response contains `result.serverInfo.name === 'odoo-mcp'`, (b) `tools/list` response contains exactly 10 tools in `result.tools`. On assertion failure, print the raw response and exit with code 1. On success print `"smoke test passed"` and exit 0. Script is manual-run only (not wired into CI). Usage comment at top of file explains required env vars.
- **Acceptance Criteria**:
  1. Running against a compiled binary with valid env vars exits with code 0 and prints "smoke test passed".
  2. Running with missing env vars exits with code 1 and includes the server's stderr output in the script's stderr.

---

## Phase 5: Polish

### T-24: README, Claude Desktop config example, and Docker Compose

- **Status**: pending
- **Wave**: 7
- **Wired**: n/a
- **Deprecates**: none
- **Dependencies**: T-22
- **Covers**: US-10 AC-1, US-10 AC-2, US-10 AC-3
- **Wire into**: n/a
- **Files**:
  - `README.md` [NEW]
  - `examples/claude-desktop-config.json` [NEW]
  - `docker-compose.yml` [NEW]
- **Description**: `README.md`: quick-start section with the `npx @netlinks/odoo-mcp` command, env var table (`ODOO_URL`, `ODOO_DB`, `ODOO_USERNAME`, `ODOO_API_KEY`, `ODOO_MCP_LOG_FILE`), Claude Desktop config snippet, tool reference table (all 10 tools), multi-company usage note, MIT license. Written in Farshid's voice — direct, no padding. `examples/claude-desktop-config.json`: a ready-to-paste MCP server config for Claude Desktop with `"command": "npx"`, `"args": ["@netlinks/odoo-mcp"]`, and all 4 required env vars shown as `"<PLACEHOLDER>"` strings. `docker-compose.yml`: `odoo` service (Odoo 19 CE image) + `db` service (Postgres 16), with a comment block explaining it is intended for future integration testing and is not wired to CI in v0.
- **Acceptance Criteria**:
  1. `README.md` contains the strings `npx @netlinks/odoo-mcp`, `ODOO_URL`, `ODOO_DB`, `ODOO_USERNAME`, and `ODOO_API_KEY`.
  2. `examples/claude-desktop-config.json` is valid JSON containing `"command": "npx"` and `"args": ["@netlinks/odoo-mcp"]`.
  3. `docker-compose.yml` defines services named `odoo` and `db`.

---

## Wave Summary

| Wave | Tasks | Can parallelize? | Notes |
|------|-------|-----------------|-------|
| 0 | T-1, T-2, T-3, T-4 | Yes — no file overlap | Shared types/errors + workspace root + both package.json stubs |
| 1 | T-5, T-8 | Yes — no file overlap | JSON-RPC transport + PII sanitizer (both depend only on T-1) |
| 2 | T-6 | n/a — single task | Auth strategy abstraction (depends on T-5) |
| 3 | T-7, T-9, T-10, T-11, T-12, T-13 | Yes — each task owns distinct files | OdooClient + odoo-mcp infrastructure (config, logger, error formatter, context, schemas) |
| 4 | T-14, T-15, T-16, T-17, T-18, T-19 | Yes — each task owns distinct files | 5 tool handlers + capability probe (all depend on T-7 and/or T-11/T-12/T-13) |
| 5 | T-20, T-21 | Yes — no file overlap | Resource publisher (depends on T-19) + tool wiring orchestrator (depends on T-14..T-18) |
| 6 | T-22 | n/a — single task | MCP server factory + CLI (depends on T-9, T-10, T-19, T-20, T-21) |
| 7 | T-23, T-24 | Yes — no file overlap | Smoke test + docs/docker |

**Threat-model coverage:**

| US threat-model criterion | Covered by |
|---------------------------|-----------|
| US-1 AC-6: API key never in stderr/errors | T-9 (config.ts), T-10 (logger.ts) |
| US-2 AC-5: http:// URL warning at startup | T-6 (auth.ts) |
| US-3 AC-6: All-probes-fail — warning + error payloads, no empty arrays | T-19 (probe.ts) |
| US-5 AC-9: Regex validation on model `^[a-z][a-z0-9_.]*$` / method `^[a-z_][a-z0-9_]*$` | T-15 (execute.ts) |
| US-7 AC-7: buildContext rejects override of uid/allowed_company_ids/company_id | T-12 (context.ts) |
| US-7 AC-8: caller allowed_company_ids must be subset of session | T-12 (context.ts), T-14–T-18 (all tool handlers) |
| US-8 AC-6: traceback only when ODOO_MCP_DEBUG=1 | T-11 (errors.ts) |
| US-8 AC-7: 30-second timeout on all JSON-RPC calls | T-5 (rpc.ts) |
| US-9 AC-7: log file created with 0o600 permissions | T-10 (logger.ts) |
| US-10 AC-6: lockfile committed, no lifecycle scripts | T-2 (root), T-3 (odoo-client), T-4 (odoo-mcp) |
